eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
server:
  port: 8762
spring:
  application:
    name: zull
  devtools:
    restart:
      enabled: true
  boot:
    admin:
      client:
        url: http://localhost:8763